# 🔐 HTTPS для лабораторной работы 6

## 📋 Требования лабораторной работы

Из описания лабораторной работы:
> "настроить виртуальную сеть ZeroTier, IP адрес HTTPS-сервера зафиксировать в коде Tauri; открывать браузер на телефоне по адресу в виртуальной сети"

## ✅ Что нужно сделать

### 1. Для веб-версии (ветка `main` или `lab_6`)

**Требование:** "открывать браузер на телефоне по адресу в виртуальной сети"

**Что нужно:**
- ✅ Настроить ZeroTier на компьютере и телефоне
- ✅ Настроить веб-сервер с HTTPS для отдачи фронтенда
- ✅ Открыть сайт на телефоне по ZeroTier IP через HTTPS

**HTTPS нужен:** ✅ **ДА** - для доступа с телефона по требованиям лабораторной работы

---

### 2. Для Tauri приложения (ветка `lab_6_tauri`)

**Требование:** "IP адрес HTTPS-сервера зафиксировать в коде Tauri"

**Что нужно:**
- ✅ Настроить ZeroTier
- ✅ Зафиксировать HTTPS IP адрес в коде Tauri приложения
- ✅ Tauri приложение должно обращаться к бэкенду по HTTPS через ZeroTier IP

**HTTPS нужен:** ✅ **ДА** - IP адрес должен быть HTTPS-сервера

---

## 🎯 Итого

**HTTPS нужен для ОБЕИХ версий:**
1. **Веб-версия** - чтобы открывать сайт на телефоне по HTTPS
2. **Tauri версия** - чтобы зафиксировать HTTPS IP в коде

---

## 📝 Что делать

### Вариант 1: Простой способ (для тестирования)

Используйте один веб-сервер для фронтенда и бэкенда:

1. **Настройте бэкенд с HTTPS:**
   - Запустите бэкенд на `0.0.0.0:443` с HTTPS
   - Используйте самоподписанный сертификат

2. **Настройте nginx для фронтенда:**
   - Отдавайте статические файлы из `dist/`
   - Проксируйте `/api` на бэкенд
   - Используйте тот же HTTPS сертификат

3. **Для Tauri:**
   - В коде зафиксируйте IP: `https://10.174.203.183:443`

### Вариант 2: Раздельные серверы

1. **Бэкенд:** `https://10.174.203.183:443` (API)
2. **Фронтенд:** `https://10.174.203.183:8443` (веб-сайт)
3. **Для Tauri:** используйте `https://10.174.203.183:443` (бэкенд)

---

## 🔧 Быстрая настройка

### Шаг 1: Создайте SSL сертификат

```bash
openssl req -x509 -newkey rsa:4096 -nodes -keyout key.pem -out cert.pem -days 365 -subj "/CN=10.174.203.183"
```

### Шаг 2: Настройте бэкенд на HTTPS

**Django:**
```python
# settings.py
ALLOWED_HOSTS = ['10.174.203.183', '0.0.0.0']

# Запуск с HTTPS
python manage.py runserver_plus --cert-file cert.pem --key-file key.pem 0.0.0.0:443
```

**Flask:**
```python
if __name__ == '__main__':
    app.run(host='0.0.0.0', port=443, ssl_context=('cert.pem', 'key.pem'))
```

### Шаг 3: Настройте nginx для фронтенда

```nginx
server {
    listen 8443 ssl;
    server_name 10.174.203.183;
    
    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;
    
    root /path/to/WEB-test-front/dist;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    location /api {
        proxy_pass https://127.0.0.1:443;
        proxy_set_header Host $host;
    }
}
```

### Шаг 4: Для Tauri (ветка `lab_6_tauri`)

Создайте файл `src/config/api.ts`:
```typescript
export const API_BASE_URL = "https://10.174.203.183:443";
```

---

## 📱 Проверка

1. **Веб-версия:**
   - Откройте на телефоне: `https://10.174.203.183:8443`
   - Должен открыться сайт

2. **Tauri:**
   - Запустите `npm run tauri:dev`
   - Проверьте, что API запросы идут на `https://10.174.203.183:443`

---

## ⚠️ Важно

- Используйте **HTTPS** (порт 443 или другой) по требованиям лабораторной работы
- Браузер покажет предупреждение о самоподписанном сертификате - это нормально
- На телефоне нажмите "Продолжить" или "Дополнительно" → "Перейти на сайт"
- Убедитесь, что firewall разрешает подключения на выбранных портах

