# ✅ Чеклист: HTTPS и доступ с телефона для ветки lab_6

## ✅ Что настроено:

### 1. Зависимости
- ✅ `vite-plugin-mkcert` установлен
- ✅ `@types/node` установлен

### 2. Сертификаты
- ✅ `ca.crt` и `ca.key` - сертификат Authority
- ✅ `cert.crt` и `cert.key` - сертификат приложения
- ✅ Приватные ключи добавлены в `.gitignore`

### 3. Конфигурация vite.config.ts
- ✅ HTTPS настроен для dev сервера (порт 5173)
- ✅ HTTPS настроен для preview режима (порт 443)
- ✅ `host: '0.0.0.0'` - для доступа с телефона через ZeroTier
- ✅ `mkcert()` плагин подключен
- ✅ Сертификаты загружаются из `cert.key` и `cert.crt`

### 4. Роутинг
- ✅ `BrowserRouter` с `basename="/WEB-test-front"`
- ✅ Service Worker настроен для PWA

---

## 🚀 Как проверить:

### 1. Запустите dev сервер:
```bash
npm run dev
```

**Ожидаемый вывод:**
```
➜  Local:   https://localhost:5173/
➜  Network: https://<ваш-ip>:5173/
```

### 2. Откройте в браузере:
```
https://localhost:5173
```

**Что проверить:**
- ✅ URL начинается с `https://`
- ✅ В адресной строке есть 🔒 (замок)
- ✅ Браузер может показать предупреждение о сертификате - это нормально, нажмите "Продолжить"

### 3. Проверьте Service Worker:
1. Откройте DevTools (F12)
2. Перейдите в **Application** → **Service Workers**
3. ✅ Должен быть зарегистрирован Service Worker

### 4. Доступ с телефона через ZeroTier:

**Шаг 1:** Узнайте ваш ZeroTier IP адрес
- В приложении ZeroTier на компьютере
- Или на сайте my.zerotier.com

**Шаг 2:** Убедитесь, что телефон подключен к той же ZeroTier сети

**Шаг 3:** Откройте на телефоне:
```
https://<ваш-zerotier-ip>:5173
```

**Например:**
```
https://10.174.203.183:5173
```

**Шаг 4:** Примите предупреждение о сертификате на телефоне
- Нажмите "Продолжить" или "Дополнительно" → "Перейти на сайт"

**Что проверить:**
- ✅ Сайт открывается на телефоне
- ✅ Интерфейс адаптивный (удобно на мобильном)
- ✅ Поиск работает
- ✅ Навигация работает
- ✅ Service Worker зарегистрирован (проверьте в DevTools на телефоне)

---

## 🔍 Проверка preview режима:

Для production сборки:

```bash
npm run build
npm run preview
```

**Ожидаемый вывод:**
```
➜  Local:   https://localhost:443/
➜  Network: https://<ваш-ip>:443/
```

Откройте на телефоне:
```
https://<ваш-zerotier-ip>:443
```

---

## ⚠️ Важные замечания:

1. **Firewall:** Убедитесь, что порты 5173 и 443 открыты в firewall
2. **ZeroTier:** Оба устройства (компьютер и телефон) должны быть в одной ZeroTier сети
3. **Сертификат:** Предупреждение о самоподписанном сертификате - это нормально для локальной разработки
4. **HTTPS обязателен:** По требованиям лабораторной работы должен использоваться HTTPS

---

## 🐛 Решение проблем:

### Проблема: "Port 5173 is already in use"
**Решение:** Остановите другой процесс на этом порту или измените порт в `vite.config.ts`

### Проблема: "Cannot find module 'vite-plugin-mkcert'"
**Решение:** 
```bash
npm install -D vite-plugin-mkcert
```

### Проблема: "ENOENT: no such file or directory, open 'cert.key'"
**Решение:** Создайте сертификаты:
```bash
npx mkcert create-ca
npx mkcert create-cert
```

### Проблема: Не могу открыть с телефона
**Решение:**
1. Проверьте, что `host: '0.0.0.0'` в `vite.config.ts`
2. Проверьте firewall - порт должен быть открыт
3. Проверьте, что оба устройства в одной ZeroTier сети
4. Используйте `https://`, а не `http://`

---

## ✅ Итоговый чеклист:

- [ ] Dev сервер запускается с HTTPS
- [ ] Сайт открывается на компьютере по `https://localhost:5173`
- [ ] Service Worker зарегистрирован
- [ ] Сайт открывается на телефоне по `https://<zerotier-ip>:5173`
- [ ] Интерфейс работает на телефоне
- [ ] Поиск и навигация работают
- [ ] Preview режим работает (опционально)

